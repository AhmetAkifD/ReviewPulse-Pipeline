<Window x:Class="ReviewHarvester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReviewHarvester"
        mc:Ignorable="d"
        Title="ReviewHarvester - Hasat Merkezi" Height="500" Width="850"
        Background="{DynamicResource PrimaryBrush}">

    <Grid Margin="25">
        <Grid.Resources>
            <Style TargetType="{x:Type GridViewColumnHeader}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource SecondaryBrush}"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Padding" Value="5,8"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="BorderBrush" Value="{DynamicResource SecondaryBrush}"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
            </Style>

            <Style TargetType="{x:Type ListViewItem}">
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListViewItem}">
                            <Border x:Name="Bd" CornerRadius="5" Background="{TemplateBinding Background}" Padding="5">
                                <GridViewRowPresenter VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#1AFFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type Button}">
                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="true">
                                    <Setter TargetName="border" Property="Opacity" Value="0.70"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Grid Margin="0,0,0,15">
                <TextBlock Text="ReviewHarvester - Bağlantı Ayarları" 
                           FontSize="20" FontWeight="Bold" 
                           Foreground="{DynamicResource TextBrush}"
                           VerticalAlignment="Center" HorizontalAlignment="Left"/>

                <Button x:Name="BtnThemeToggle" Click="BtnThemeToggle_Click"
                        Width="45" Height="40" HorizontalAlignment="Right"
                        Background="{DynamicResource SecondaryBrush}">
                    <TextBlock x:Name="TxtThemeIcon" Text="🌙" FontSize="18" VerticalAlignment="Center"/>
                </Button>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="BtnLoadTxt" Content="TXT Yükle" Width="120" Height="40" 
                        Click="BtnLoadTxt_Click" Background="{DynamicResource SecondaryBrush}" Margin="0,0,12,0"/>

                <TextBox Grid.Column="1" x:Name="TxtUrl" Padding="12,8" FontSize="14" VerticalContentAlignment="Center" 
                         Background="#1AFFFFFF" Foreground="{DynamicResource TextBrush}" BorderThickness="1" 
                         BorderBrush="{DynamicResource SecondaryBrush}">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Grid.Column="1" IsHitTestVisible="False" Text="Link giriniz" 
                           VerticalAlignment="Center" HorizontalAlignment="Left" Margin="14,0,0,0" Foreground="Gray" Opacity="0.8">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=TxtUrl}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="BtnStart" Content="Hasadı Başlat" Width="130" Height="40" 
                    Click="BtnStart_Click" Background="{DynamicResource SecondaryBrush}"/>

            <Button Grid.Column="1" x:Name="BtnCsvManager" Content="CSV Yöneticisi" Width="130" Height="40" 
                    Margin="12,0,0,0" Click="BtnCsvManager_Click" Background="{DynamicResource SecondaryBrush}"/>

            <ProgressBar Grid.Column="2" x:Name="PrgStatus" Height="10" Margin="20,0" VerticalAlignment="Center" 
                         Foreground="{DynamicResource SecondaryBrush}" Background="#1AFFFFFF" BorderThickness="0"/>

            <Button Grid.Column="3" x:Name="BtnSave" Content="CSV Kaydet" Width="130" Height="40" 
                    Click="BtnSave_Click" Background="{DynamicResource AccentBrush}"/>
        </Grid>

        <ListView Grid.Row="2" x:Name="LstReviews" ItemsSource="{Binding HarvestedReviews}" 
                  Background="Transparent" BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Kullanıcı" DisplayMemberBinding="{Binding User}" Width="120"/>
                    <GridViewColumn Header="Puan" DisplayMemberBinding="{Binding Rating}" Width="60"/>
                    <GridViewColumn Header="Yorum" DisplayMemberBinding="{Binding Comment}" Width="580"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StatusBar Grid.Row="3" Background="Transparent" Margin="0,15,0,0">
            <TextBlock x:Name="TxtStatus" Text="Hazır." Foreground="{DynamicResource TextBrush}" Opacity="0.6"/>
        </StatusBar>
    </Grid>
</Window>